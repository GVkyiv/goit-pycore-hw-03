# goit-pycore-hw-03

## Task 1

У проєкті реалізовано функцію `get_days_from_today(date)` у файлі `task1.py`.

### Використання

```python
from task1 import get_days_from_today

print(get_days_from_today("2021-10-09"))
```

## Відкрити у VS Code

1. Відкрийте VS Code.
2. `File -> Open Folder...`
3. Оберіть папку `goit-pycore-hw-03`.

## Завантажити на GitHub

```bash
git init
git add .
git commit -m "Add Task 1"
git branch -M main
git remote add origin <YOUR_GITHUB_REPO_URL>
git push -u origin main
```
